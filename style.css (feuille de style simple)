
:root{
  --bg:#0f1220; --card:#171a2b; --border:#272b41; --text:#e7ecff;
  --muted:#a9b6d3; --accent:#4cc2ff; --accent2:#7cf29b; --ink:#04131c;
}
*{box-sizing:border-box}
body{margin:0;background:var(--bg);color:var(--text);
     font:16px/1.6 system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial}
.wrap{max-width:1000px;margin:0 auto;padding:28px}
.site-header{padding:18px 28px;border-bottom:1px solid #2a2f45}
h1{margin:0 0 6px;font-size:28px}
.tagline{color:var(--muted)}
.lang-switch{margin-top:8px}
.btn{display:inline-block;background:var(--accent);color:var(--ink);
     text-decoration:none;padding:10px 14px;border-radius:8px;font-weight:700;margin-right:8px}
.btn.alt{background:transparent;border:1px solid var(--accent);color:var(--text)}
.card{background:var(--card);border:1px solid var(--border);border-radius:12px;padding:16px;margin:18px 0}
.bullets{margin:0;padding-left:18px}
.site-footer{margin-top:24px;padding:18px 28px;border-top:1px solid #2a2f
